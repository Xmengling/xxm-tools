/**
 * @description 小写字母转为大写
 * @author yuanyingying
 * @date 26/11/2021
 * @export
 * @param str
 * @returns {*} 返回转换后的字符串
 */
export function toUpperCase(str: string): string {
  return str.toUpperCase();
}

export default {
  toUpperCase,
};
